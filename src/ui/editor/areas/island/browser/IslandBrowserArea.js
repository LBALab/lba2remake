import {makeOutlinerArea} from '../../utils/outliner';

const IslandBrowserArea = makeOutlinerArea('islands_list', 'Islands', {
    name: 'Islands',
    children: []
});

export default IslandBrowserArea;
