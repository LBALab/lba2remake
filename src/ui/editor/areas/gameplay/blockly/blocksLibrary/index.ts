import {
    lba_behaviour,
    lba_behaviour_init,
    lba_set_behaviour,
    lba_set_behaviour_obj
} from './life/structural';
import {
    lba_if,
    lba_swif,
    lba_oneif,
    lba_switch,
    lba_dummy_operand,
    lba_and,
    lba_or
} from './life/control';
import {
    lba_set_varscene,
    lba_set_vargame,
    lba_set_anim,
    lba_set_anim_obj
} from './life/actions';
import {
    lba_distance,
    lba_collision,
    lba_collision_obj,
    lba_zone,
    lba_zone_obj
} from './life/conditions';
import {
    lba_track,
} from './move/structural';
import {
    lba_wait_sec,
    lba_wait_anim,
    lba_goto_point
} from './move/actions';

export default {
    lba_behaviour,
    lba_behaviour_init,
    lba_set_behaviour,
    lba_set_behaviour_obj,
    lba_if,
    lba_swif,
    lba_oneif,
    lba_switch,
    lba_dummy_operand,
    lba_and,
    lba_or,
    lba_distance,
    lba_collision,
    lba_collision_obj,
    lba_zone,
    lba_zone_obj,
    lba_set_varscene,
    lba_set_vargame,
    lba_set_anim,
    lba_set_anim_obj,
    lba_track,
    lba_wait_sec,
    lba_wait_anim,
    lba_goto_point
  };
