import {makeOutlinerArea} from '../../utils/outliner';

const AnimsBrowserArea = makeOutlinerArea('anims_list', 'Animations', {
    name: 'Animations',
    children: []
});

export default AnimsBrowserArea;
